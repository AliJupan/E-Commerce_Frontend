import { Typography, Button } from "@mui/material";

function Dashboard() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Dashboard 📊
      </Typography>
      <Button variant="contained">Example Action</Button>
    </div>
  );
}

export default Dashboard;
